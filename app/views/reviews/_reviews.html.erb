<% reviews.each do |review| %>
  <div class="card my-2">
    <div class="card-body">
      <h5 class="card-title">Calificación: <%= review.rating %></h5>
      <p class="card-text">Comentario: <%= review.comment %></p>
      <p class="card-text">Emisor: <%= User.find(review.user_reviewer_id).email %></p>
      <p class="card-text">Receptor: <%= User.find(review.user_reviewed_id).email %></p>

    </div>
  </div>
<% end %>
