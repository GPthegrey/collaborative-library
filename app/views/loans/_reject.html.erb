<%= simple_form_for loan, url: reject_loan_path(loan), method: :patch do |f| %>
  <%= f.hidden_field :loan_id, value: loan.id %>
  <%= f.button :submit, 'Rechazar', class: 'btn btn-danger my-2' %>
<% end %>
