<%= simple_form_for loan, url: return_loan_path(loan), method: :patch do |f| %>
  <%= f.hidden_field :loan_id, value: loan.id %>
  <%= f.button :submit, 'Libro devuelto/Prestamo concluido', class: 'btn btn-primary my-2' %>
<% end %>
